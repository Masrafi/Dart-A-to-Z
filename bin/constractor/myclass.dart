class MyClass {
  var name, age;

  MyClass(var a, var b) {
    //constractor method, retirn type thake na, no need to call
    name = a;
    age = b;
  }

  void mas() {
    print("Now it run");
  }
}
